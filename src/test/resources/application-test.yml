spring:
  datasource:
    url: jdbc:h2:mem:auction_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
  task:
    scheduling:
      enabled: false

app:
  jwt:
    secret: test-secret-key-that-is-long-enough-for-hs256
    access-token-minutes: 15
  cors:
    allowed-origins: http://localhost:3000
